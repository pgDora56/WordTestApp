<Window x:Class="WordTestApp.MissList"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WordTestApp"
        mc:Ignorable="d"
        Title="MissList" Height="300" Width="600">
    <Grid>
        <ListView x:Name="listView" HorizontalAlignment="Left" Height="189" VerticalAlignment="Top" Width="305">
            <ListView.View>
                <GridView>

                    <GridViewColumn DisplayMemberBinding="{Binding Path=Japanese}" 
                      Header="Japanese" Width="100"/>

                    <GridViewColumn DisplayMemberBinding="{Binding Path=English}" 
                      Header="English" Width="100">
                    </GridViewColumn>

                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Window>
